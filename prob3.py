from datetime import timedelta
d = timedelta(microseconds=-1)
#print(d.days, d.seconds, d.microseconds)

correctTime = timedelta(days=100, hours=10, minutes=13)
#print(correctTime.days, correctTime.seconds, correctTime.microseconds)